extends layout

block content
  h1= title
  p Welcome to #{title}
  if(!user)
    a(href='/login') Login
    br
    a(href='/register') Register
  if(user)
    p You are currently logged in as #{user.username}
    a(href="/department") Take me to department 
    br
  a(href="/logout") Logout
  //- <button type="button" class="btn btn-secondary">Press Me</button>
  //- div#reduce
  //-  Reduce the value.
  //-  script.
  //-   var count = 20;
  //-   var change = -1;
  //-   function doReduce() {
  //-     count += change;
  //-     change--;
  //-     document.getElementById("reduce").innerHTML = "Count " + count + " with change " + change;
  //-   }
  //- button(onclick="doReduce()") Click me
  //- <br/>
  //- a(href="http://localhost:3000/department") Departments
  